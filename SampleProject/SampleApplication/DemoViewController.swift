//
//  DemoViewController.swift
//  SampleApplication
//
//  Created by Talari Gopi on 12/12/24.
//

import UIKit

class DemoViewController: UIViewController {
    
    override func viewDidLoad() {
        super.viewDidLoad()
        /*let sb = UIStoryboard(name: "Main", bundle: Bundle.main)  // Reference the storyboard in your demo app
        let controller = sb.instantiateViewController(identifier: "ViewController")  // Use the Storyboard ID
        self.navigationController?.pushViewController(controller, animated: true)*/
    }
    
    @IBAction func buttonTapped(_ sender: UIButton) {
        print("fkjhgf")
        /*let sb = UIStoryboard(name: "Main", bundle: Bundle.main)  // Reference the storyboard in your demo app
        let controller = sb.instantiateViewController(identifier: "ViewController")  // Use the Storyboard ID
        self.navigationController?.pushViewController(controller, animated: true)*/
      //  self.navigateToPackage()
    }
}
